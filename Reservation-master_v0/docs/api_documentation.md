# 接口文档（已废弃）

旧版项目曾提供 FastAPI REST 接口以及自动任务管理接口，相关内容已在当前版本中移除。我们保留此文档文件仅用于提示：

- `/api/v1/*`、`/auto-bookings` 等本地端点已不再可用。
- 若您仍需要这些能力，请回退到历史版本或自行重建 FastAPI 服务。
- 目前生效的接口说明请参考 [docs/api.md](api.md)，其中记录了脚本直接调用的学校官方接口。

如果你正在阅读旧文档，请务必更新到新的脚本化流程，避免在部署时产生混淆。
