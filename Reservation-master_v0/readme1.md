# 
用新的 Python 版本创建虚拟环境：
```powershell
# 假设 python 命令现在指向 3.13
python -m venv .venv
```
激活新的虚拟环境：
```powershell
.venv\Scripts\Activate.ps1
```
重新安装依赖：
```powershell
pip install -r requirements.txt
```
